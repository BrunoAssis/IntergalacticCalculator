module IntergalacticCalculator
  class Command
    PATTERN = //

    def initialize(command_text)
      @command_text = command_text
    end

    def execute!(options = {})
      nil
    end
  end
end
